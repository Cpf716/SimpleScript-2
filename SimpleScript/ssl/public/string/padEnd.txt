//  Author: Corey Ferguson
//  Date:   August 3, 2023
//  File:   padEnd.txt
//

include "@/error/expectError"
include "@/error/rangeError"
include "@/string/string"

func main
    if nrows(argv) !== 4
        throw expectError("4 argument(s), got {}" format (nrows(argv) - 1))
    end if

    text = ((argv row 1) slice 1, argv cell 1, 0)

    if typeOf text !== "string"
        typeError(typeOf text, "string")
    end if

    if text === null
        throw "null"
    end if

    tgtLen = ((argv row 2) slice 1, argv cell 2, 0)

    if subtypeOf tgtLen !== "int"
        typeError(subtypeOf tgtLen, "int")
    end if

    if tgtLen < 0
        rangeError(string(tgtLen))
    end if

    padStr = ((argv row 3) slice 1, argv cell 3, 0)

    if typeOf padStr !== "string"
        typeError(typeOf padStr, "string")
    end if

    if padStr === null
        throw "null"
    end if

    while count text < tgtLen
        text += padStr
    end while

    return text
end func

return main()
